# NER Landslide EWS — backend
#
# Supported: Python 3.11 – 3.14
#
# Version FLOORS, not exact pins. Exact pins looked reproducible but they silently
# encode "whatever Python existed when this was written": pydantic-core did not ship
# a Python 3.14 wheel until 2.35.0, so a hard pin to 2.23.4 made pip try to compile
# Rust from source on any newer interpreter and fail against a PyO3 version wall.
# Floors let a new interpreter pick up a wheel that supports it.
#
# For byte-identical installs across machines, generate a lock file from this:
#   pip install pip-tools && pip-compile requirements.txt -o requirements.lock

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0.35,<3.0
pydantic>=2.12,<3.0          # 2.12 is the first release pulling a 3.14-capable core
python-multipart>=0.0.9      # multipart field-report uploads
httpx>=0.27,<1.0             # SMS gateway + IMD pull

# GIS raster + terrain derivative loading (data/gis/*.npz)
numpy>=1.26

# dev
pytest>=8.3
